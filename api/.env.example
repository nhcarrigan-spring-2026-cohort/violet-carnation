# Example environment variables for local development.
# Copy to .env and adjust as needed.

# Signing secret for JWT tokens.
# In production this must be a long, random string.
# Generate one with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=dev-secret-key-change-in-production

# Application environment. Set to "production" or "staging" on live servers.
# When ENV != "development" the server will refuse to start if SECRET_KEY is
# still set to the default value above.
ENV=development

# Comma-separated list of allowed CORS origins.
# Example: https://app.example.com,https://www.example.com
# Leave unset (or empty) to allow all origins in local development ONLY.
ALLOWED_ORIGINS=
