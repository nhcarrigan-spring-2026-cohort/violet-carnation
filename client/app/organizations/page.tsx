/**
 * This page is the main landing page for organizations.
 *
 * Should show:
 * - the organizations the user is a part of
 * - organizations the user can join
 * - ability to create a new organization
 * - a search bar to find organizations
 * - filters to narrow down the list of organizations based on high level criteria (e.g. location, category, etc.)
 */
export default function OrganizationsPage() {
  return <div>OrganizationsPage</div>;
}
