/**
 * This page is for viewing and managing users of a specific organization.
 *
 * Only admins can access this page and manipulate users.
 *
 * At this current time admins can search and add users to the organization, remove and change their permissions.
 *
 * Admins can change their own permissions and remove themselves after surfacing a warning,
 * but the admin who created the organization CANNOT downgrade their permission or remove themselves.
 */
export default async function OrganizationUsersPage() {
  return <div>OrganizationUsersPage</div>;
}
