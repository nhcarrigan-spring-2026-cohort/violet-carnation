/**
 * This page is for editing a specific organization.
 *
 * This can only be done by an admin user.
 *
 * Only the admin user who is also the creator of the organization can delete the organization, no other admin can
 * perform this action.
 */
export default async function EditOrganizationPage() {
  return <div>OrganizationEditPage</div>;
}
